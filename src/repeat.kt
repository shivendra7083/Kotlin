fun main () {

    // Repeat
    // The repeat loop in Kotlin is a simple way to execute a block of code multiple times without needing an explicit counter.


    repeat(5) {
        println("Hello, Kotlin!")
        // The loop runs 5 times and prints "Hello, Kotlin!" each time.
    }

}